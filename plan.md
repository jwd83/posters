make a python script called 01_clone.py that clones that clones https://github.com/jwd83/posterdb into ./tmp/.
make a python script called 02_move.py that deletes ./raw/ and replaces it with  ./tmp/raw/
make a python script called 03_catalog.py that generates a catalog.txt that lists every image file in the /raw/ folder. use \n for newline